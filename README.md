# Plato's Planet

Minimal demo app: Express backend serving a static frontend.

Quickstart

1. Open a terminal in the project folder:

```powershell
cd "C:\Users\kabir\Plato claude app\platos-planet"
npm install
npm start
```

2. Open http://localhost:3000 in your browser.
